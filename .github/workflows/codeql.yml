name: "Tag :any"

on: [pull_request]
  

jobs:
  tagger:
    name: Analyze
    permissions: 
      pull-requests: write
      contents: read
    runs-on: ubuntu-latest
    steps:
    - name: Checkout repository
      uses: actions/checkout@v3

    - name: Check (any) type
      uses: nguyenvanuyn96/str-find-action@master
      id: any-type
      with:
        # text: ${{ github.event. }}
        find: "any"
        include: '.*.ts'
    
      # uses: vrsoftbr/search-text@v1
      # id: any-type
      # with:
      #   text: 'any'
      #   include: '*.ts'
      #   exclude-dir: '{node_modules,coverage}'
      #   error-on-find: false
    - id: add-label #uses: actions-ecosystem/action-add-labels@v1
      # if: ${{ steps.any-type.outputs.count > 0 }}
      run: echo "The count is ${{ steps.any-type.outputs.resultArray }}"
      # with:
      #   github_token: ${{ secrets.GITHUB_TOKEN }}
      #   labels: any
        
